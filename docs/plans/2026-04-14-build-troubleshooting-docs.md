# 构建排障文档固化实现计划

> 执行说明：按任务顺序逐项实现，并在每个关键步骤后执行可验证检查。

**目标:** 把 Avalonia 构建排障经验固化到正式运维文档、README 入口和经验归档中，避免后续重复踩坑。

**架构:** 正式规则放在 `docs/05-operations/`，README 只保留短入口和推荐命令，`docs/plans/` 补一份经验归档说明这次沉淀的背景与结论。内容只覆盖构建链与排障方法，不涉及站点业务规则。

**技术栈:** Markdown、.NET CLI、Avalonia

---

### 任务 1: 新增正式排障文档

**文件:**
- 新增: `docs/05-operations/AVALONIA_BUILD_TROUBLESHOOTING.md`

**步骤 1: 编写失败测试**

此任务为文档固化，不新增自动化测试。

**步骤 2: 运行测试确认失败**

不适用。

**步骤 3: 编写最小实现**

写入以下内容：
- 构建链的两个已知问题
- 根因说明
- 推荐串行验证命令
- 禁止并行执行 Avalonia `build/test`
- `Directory.Build.targets` 禁用 Avalonia telemetry 的原因
- 遇到“无错误失败”时的最小排障顺序

**步骤 4: 回跑测试确认通过**

执行文档语言检查与最终构建验证。

### 任务 2: README 增加短入口

**文件:**
- 修改: `digital-intelligence-bridge/README.md`

**步骤 1: 编写失败测试**

此任务为文档固化，不新增自动化测试。

**步骤 2: 运行测试确认失败**

不适用。

**步骤 3: 编写最小实现**

增加“本地构建与验证建议”小节：
- 推荐 `-m:1`
- 强调顺序执行
- 链接正式排障文档

**步骤 4: 回跑测试确认通过**

执行文档语言检查与最终构建验证。

### 任务 3: 补经验归档

**文件:**
- 新增: `docs/plans/2026-04-14-build-troubleshooting-retrospective.md`
- 修改: `docs/plans/README.md`

**步骤 1: 编写失败测试**

此任务为文档固化，不新增自动化测试。

**步骤 2: 运行测试确认失败**

不适用。

**步骤 3: 编写最小实现**

写入：
- 本次问题现象
- 根因拆分
- 已落地修复
- 后续执行约束

并在 `docs/plans/README.md` 中把它归类到“历史记录文档”。

**步骤 4: 回跑测试确认通过**

执行文档语言检查与最终构建验证。
